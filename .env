# .env file

# JWT Secret Key for signing the JWT token (This should be kept secret and unique)
JWT_SECRET=supersecretkey12345

# The environment type (should be 'development' or 'production')
NODE_ENV=development
SERVER_PORT=3000

# This was inserted by `prisma init`:
# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="postgresql://postgres:qwerty@postgres:5432/enaam?schema=public"
POSTGRES_USERNAME=postgres
POSTGRES_PASSWORD=qwerty
POSTGRES_DATABASE=enaam

MACHINE_ADMINER_PORT=8080
ADMINER_PORT=8080